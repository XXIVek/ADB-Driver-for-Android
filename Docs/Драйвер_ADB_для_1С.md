# Драйвер ADB для 1С — Журнал изменений

## 30.06.2026 — Версия 1.0.0.4

### Скомпилированные DLL:
- **1C/Release/Win64/Release/ADBFileDriver_Win64.dll** — собрано ✅
- **1C/Release/Win32/Release/ADBFileDriver_Win32.dll** — собрано ✅

### Добавлено новое свойство:

**СостояниеADBСервера** (ADBServerState) — тип Bool
- `true` — ADB сервер запущен и работает
- `false` — ADB сервер не запущен или ошибка

### Изменения в логике работы:

1. **При инициализации** — автоматически запускается `adb start-server` и проверяется состояние
2. **При чтении любого свойства** — обновляется актуальное состояние сервера
3. **Если ADB сервер не запущен** — `СписокУстройств` возвращает `[]`

### Итоговый сценарий работы:

```
1. Init() → adb start-server → CheckADBServer()
2. СостояниеADBСервера → true/false
3. СписокУстройств → если сервер не запущен → []
4. ПодключитьУстройство() → adb devices
5. Работа с файлами
6. ОтключитьУстройство()
```

## Подключение в 1С (версия 1.0.0.4):
```bsl
// Для 64-битной 1С:
Результат = ПодключитьВнешнююКомпоненту(
    "J:\Visual studio prog\ADB Driver for Android\1C\Release\Win64\Release\ADBFileDriver_Win64.dll",
    "ADBFileDriver",
    ТипВнешнейКомпоненты.Native
);

// Проверка состояния ADB сервера
Если Результат.СостояниеADBСервера = Истина Тогда
    Сообщить("ADB сервер работает");
    // Теперь можно работать с устройствами
    Список = Результат.СписокУстройств;
Иначе
    Сообщить("ADB сервер не запущен!");
КонецЕсли;
```

## Список команд компоненты (версия 1.0.0.4):

### Свойства (3):
| Имя (RU) | Имя (EN) | Тип | Описание |
|----------|----------|-----|----------|
| СтатусПодключения | Status | Строка | Текущий статус |
| СписокУстройств | DeviceList | Строка (JSON) | JSON список устройств |
| СостояниеADBСервера | ADBServerState | Bool | true/false |

### Методы (5):
| Имя (RU) | Имя (EN) | Параметры | Возврат | Описание |
|----------|----------|-----------|---------|----------|
| ПодключитьУстройство | Connect | нет | Истина/Ложь | Подключение к первому устройству |
| ОтключитьУстройство | Disconnect | нет | Истина/Ложь | Отключение |
| ЗаписатьНаТелефон | PushFile | (ПутьНаПК, ПутьНаТелефоне) | Истина/Ложь | Копирование на телефон |
| ПрочитатьСТелефона | PullText | (ПутьНаТелефоне) | Строка | Чтение текстового файла |
| ПолучитьСписокУстройств | GetDeviceList | нет | Строка (JSON) | Список устройств |